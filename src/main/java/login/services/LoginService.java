package login.services;

public class LoginService {
    // create local repository variables
    // create constructor

    public String execute(String name) { // throws Exception
        return "hello, " + name;
    }
}
